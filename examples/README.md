# Fulus Examples

This directory contains examples demonstrating how to use the `fulus` library.

## Available Examples

- [api](./api): A real-world example demonstrating how to use `fulus` in an HTTP JSON API context (handling carts, checkout, JSON marshaling/unmarshaling, tax calculation).
- [codegen](./codegen): An example demonstrating currency configuration and code generation.
